{
  "opencollection": "1.0.0",
  "info": {
    "name": "Salesforce Bulk API 2.0 Abort AppMenu API",
    "version": "v63.0"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "AppMenu",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Salesforce Get the App Switcher Menu",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://{instance}.salesforce.com/services/data/v{version}/jobs/appMenu/AppSwitcher"
          },
          "docs": "Returns the list of applications available in the Salesforce App Switcher menu for the currently authenticated user. Each entry includes the app label, icon URL, and navigation URL. Use this to build custom navigation or app launchers in connected applications.\n"
        }
      ]
    }
  ],
  "bundled": true
}