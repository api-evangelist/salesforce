naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Quote
  description: 'Salesforce — Quote. 4 operations. Lead operation: Salesforce Save Quote Api. Self-contained Naftiko capability
    covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Quote
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-quote
    baseUri: https://login.salesforce.com
    description: Salesforce — Quote business capability. Self-contained, no shared references.
    resources:
    - name: apexrest-SBQQ-ServiceRouter
      path: /apexrest/SBQQ/ServiceRouter
      operations:
      - name: savequoteapi
        method: POST
        description: Salesforce Save Quote Api
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Content-Type
          in: header
          type: string
          required: true
      - name: readquoteapi
        method: GET
        description: Salesforce Read Quote Api
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: reader
          in: query
          type: string
          required: true
        - name: uid
          in: query
          type: string
          required: true
        - name: Content-Type
          in: header
          type: string
          required: true
    - name: data-v64.0-actions-standard-createOrderFromQuote
      path: /data/v64.0/actions/standard/createOrderFromQuote
      operations:
      - name: createorderfromquote
        method: POST
        description: Salesforce Create Order From Quote
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: data-v64.0-commerce-quotes-actions-place
      path: /data/v64.0/commerce/quotes/actions/place
      operations:
      - name: createorupdatequote
        method: POST
        description: Salesforce Create or Update Quote
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-quote-rest
    port: 8080
    description: REST adapter for Salesforce — Quote. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/apexrest/sbqq/servicerouter
      name: apexrest-sbqq-servicerouter
      description: REST surface for apexrest-SBQQ-ServiceRouter.
      operations:
      - method: POST
        name: savequoteapi
        description: Salesforce Save Quote Api
        call: salesforce-quote.savequoteapi
        with:
          Content-Type: rest.Content-Type
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: readquoteapi
        description: Salesforce Read Quote Api
        call: salesforce-quote.readquoteapi
        with:
          reader: rest.reader
          uid: rest.uid
          Content-Type: rest.Content-Type
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/actions/standard/createorderfromquote
      name: data-v64-0-actions-standard-createorderfromquote
      description: REST surface for data-v64.0-actions-standard-createOrderFromQuote.
      operations:
      - method: POST
        name: createorderfromquote
        description: Salesforce Create Order From Quote
        call: salesforce-quote.createorderfromquote
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/commerce/quotes/actions/place
      name: data-v64-0-commerce-quotes-actions-place
      description: REST surface for data-v64.0-commerce-quotes-actions-place.
      operations:
      - method: POST
        name: createorupdatequote
        description: Salesforce Create or Update Quote
        call: salesforce-quote.createorupdatequote
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-quote-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Quote. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-save-quote-api
      description: Salesforce Save Quote Api
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-quote.savequoteapi
      with:
        Content-Type: tools.Content-Type
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-read-quote-api
      description: Salesforce Read Quote Api
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-quote.readquoteapi
      with:
        reader: tools.reader
        uid: tools.uid
        Content-Type: tools.Content-Type
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-create-order-quote
      description: Salesforce Create Order From Quote
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-quote.createorderfromquote
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-create-update-quote
      description: Salesforce Create or Update Quote
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-quote.createorupdatequote
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
