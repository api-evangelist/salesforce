naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Failed
  description: 'Salesforce — Failed. 1 operations. Lead operation: Salesforce Get Job Failed Record Results. Self-contained
    Naftiko capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Failed
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-failed
    baseUri: https://login.salesforce.com
    description: Salesforce — Failed business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-jobs-ingest-failedResults
      path: /data/v64.0/jobs/ingest//failedResults
      operations:
      - name: getjobfailedrecordresults
        method: GET
        description: Salesforce Get Job Failed Record Results
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-failed-rest
    port: 8080
    description: REST adapter for Salesforce — Failed. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/data/v64-0/jobs/ingest/failedresults
      name: data-v64-0-jobs-ingest-failedresults
      description: REST surface for data-v64.0-jobs-ingest-failedResults.
      operations:
      - method: GET
        name: getjobfailedrecordresults
        description: Salesforce Get Job Failed Record Results
        call: salesforce-failed.getjobfailedrecordresults
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-failed-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Failed. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-get-job-failed-record
      description: Salesforce Get Job Failed Record Results
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-failed.getjobfailedrecordresults
      outputParameters:
      - type: object
        mapping: $.
