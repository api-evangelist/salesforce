naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Revoke
  description: 'Salesforce — Revoke. 4 operations. Lead operation: Salesforce Oauth User Tokens Revoke. Self-contained Naftiko
    capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Revoke
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-revoke
    baseUri: https://login.salesforce.com
    description: Salesforce — Revoke business capability. Self-contained, no shared references.
    resources:
    - name: services-data-v64.0-apps-oauth-usage-tokens
      path: /services/data/v64.0/apps/oauth/usage///tokens
      operations:
      - name: oauthusertokensrevoke
        method: DELETE
        description: Salesforce Oauth User Tokens Revoke
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: Replace <token> with your access token
          required: true
    - name: services-data-v64.0-apps-oauth-usage-tokens
      path: /services/data/v64.0/apps/oauth/usage//tokens
      operations:
      - name: oauthtokensrevoke
        method: DELETE
        description: Salesforce Oauth Tokens Revoke
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: Replace <token> with your access token
          required: true
    - name: services-data-v64.0-apps-oauth-usage-tokens
      path: /services/data/v64.0/apps/oauth/usage/tokens/
      operations:
      - name: oauthtokenrevoke
        method: DELETE
        description: Salesforce Oauth Token Revoke
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: Replace <token> with your access token
          required: true
    - name: services-oauth2-revoke
      path: /services/oauth2/revoke
      operations:
      - name: revoketoken
        method: POST
        description: Salesforce Revoke Token
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-revoke-rest
    port: 8080
    description: REST adapter for Salesforce — Revoke. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/services/data/v64-0/apps/oauth/usage/tokens
      name: services-data-v64-0-apps-oauth-usage-tokens
      description: REST surface for services-data-v64.0-apps-oauth-usage-tokens.
      operations:
      - method: DELETE
        name: oauthusertokensrevoke
        description: Salesforce Oauth User Tokens Revoke
        call: salesforce-revoke.oauthusertokensrevoke
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/services/data/v64-0/apps/oauth/usage/tokens
      name: services-data-v64-0-apps-oauth-usage-tokens
      description: REST surface for services-data-v64.0-apps-oauth-usage-tokens.
      operations:
      - method: DELETE
        name: oauthtokensrevoke
        description: Salesforce Oauth Tokens Revoke
        call: salesforce-revoke.oauthtokensrevoke
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/services/data/v64-0/apps/oauth/usage/tokens
      name: services-data-v64-0-apps-oauth-usage-tokens
      description: REST surface for services-data-v64.0-apps-oauth-usage-tokens.
      operations:
      - method: DELETE
        name: oauthtokenrevoke
        description: Salesforce Oauth Token Revoke
        call: salesforce-revoke.oauthtokenrevoke
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/services/oauth2/revoke
      name: services-oauth2-revoke
      description: REST surface for services-oauth2-revoke.
      operations:
      - method: POST
        name: revoketoken
        description: Salesforce Revoke Token
        call: salesforce-revoke.revoketoken
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-revoke-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Revoke. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-oauth-user-tokens-revoke
      description: Salesforce Oauth User Tokens Revoke
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: salesforce-revoke.oauthusertokensrevoke
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-oauth-tokens-revoke
      description: Salesforce Oauth Tokens Revoke
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: salesforce-revoke.oauthtokensrevoke
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-oauth-token-revoke
      description: Salesforce Oauth Token Revoke
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: salesforce-revoke.oauthtokenrevoke
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-revoke-token
      description: Salesforce Revoke Token
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-revoke.revoketoken
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
