naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Credentials
  description: 'Salesforce — Credentials. 4 operations. Lead operation: Salesforce List External Credentials. Self-contained
    Naftiko capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Credentials
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-credentials
    baseUri: https://login.salesforce.com
    description: Salesforce — Credentials business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-named-credentials-external-credentials
      path: /data/v64.0/named-credentials/external-credentials
      operations:
      - name: listexternalcredentials
        method: GET
        description: Salesforce List External Credentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: data-v64.0-named-credentials-external-credentials-DEVELOPER_NAME
      path: /data/v64.0/named-credentials/external-credentials/{DEVELOPER_NAME}
      operations:
      - name: getexternalcredentialsbydevelopername
        method: GET
        description: Salesforce Get External Credentials by Developer Name
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: DEVELOPER_NAME
          in: path
          type: string
          required: true
    - name: data-v64.0-named-credentials-named-credential-setup
      path: /data/v64.0/named-credentials/named-credential-setup
      operations:
      - name: listnamedcredentials
        method: GET
        description: Salesforce List Named Credentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: data-v64.0-tooling-query
      path: /data/v64.0/tooling/query
      operations:
      - name: listnamedcredentials
        method: GET
        description: Salesforce List Named Credentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: q
          in: query
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-credentials-rest
    port: 8080
    description: REST adapter for Salesforce — Credentials. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/data/v64-0/named-credentials/external-credentials
      name: data-v64-0-named-credentials-external-credentials
      description: REST surface for data-v64.0-named-credentials-external-credentials.
      operations:
      - method: GET
        name: listexternalcredentials
        description: Salesforce List External Credentials
        call: salesforce-credentials.listexternalcredentials
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/named-credentials/external-credentials/{developer-name}
      name: data-v64-0-named-credentials-external-credentials-developer-name
      description: REST surface for data-v64.0-named-credentials-external-credentials-DEVELOPER_NAME.
      operations:
      - method: GET
        name: getexternalcredentialsbydevelopername
        description: Salesforce Get External Credentials by Developer Name
        call: salesforce-credentials.getexternalcredentialsbydevelopername
        with:
          DEVELOPER_NAME: rest.DEVELOPER_NAME
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/named-credentials/named-credential-setup
      name: data-v64-0-named-credentials-named-credential-setup
      description: REST surface for data-v64.0-named-credentials-named-credential-setup.
      operations:
      - method: GET
        name: listnamedcredentials
        description: Salesforce List Named Credentials
        call: salesforce-credentials.listnamedcredentials
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/tooling/query
      name: data-v64-0-tooling-query
      description: REST surface for data-v64.0-tooling-query.
      operations:
      - method: GET
        name: listnamedcredentials
        description: Salesforce List Named Credentials
        call: salesforce-credentials.listnamedcredentials
        with:
          q: rest.q
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-credentials-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Credentials. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-list-external-credentials
      description: Salesforce List External Credentials
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-credentials.listexternalcredentials
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-get-external-credentials-developer
      description: Salesforce Get External Credentials by Developer Name
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-credentials.getexternalcredentialsbydevelopername
      with:
        DEVELOPER_NAME: tools.DEVELOPER_NAME
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-list-named-credentials
      description: Salesforce List Named Credentials
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-credentials.listnamedcredentials
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-list-named-credentials-2
      description: Salesforce List Named Credentials
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: salesforce-credentials.listnamedcredentials
      with:
        q: tools.q
      outputParameters:
      - type: object
        mapping: $.
