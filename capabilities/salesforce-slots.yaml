naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Slots
  description: 'Salesforce — Slots. 1 operations. Lead operation: Salesforce Get Appointment Slots. Self-contained Naftiko
    capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Slots
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-slots
    baseUri: https://login.salesforce.com
    description: Salesforce — Slots business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-scheduling-getAppointmentSlots
      path: /data/v64.0/scheduling/getAppointmentSlots
      operations:
      - name: getappointmentslots
        method: POST
        description: Salesforce Get Appointment Slots
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-slots-rest
    port: 8080
    description: REST adapter for Salesforce — Slots. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/data/v64-0/scheduling/getappointmentslots
      name: data-v64-0-scheduling-getappointmentslots
      description: REST surface for data-v64.0-scheduling-getAppointmentSlots.
      operations:
      - method: POST
        name: getappointmentslots
        description: Salesforce Get Appointment Slots
        call: salesforce-slots.getappointmentslots
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-slots-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Slots. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-get-appointment-slots
      description: Salesforce Get Appointment Slots
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: salesforce-slots.getappointmentslots
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
