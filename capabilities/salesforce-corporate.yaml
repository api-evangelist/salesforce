naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Corporate
  description: 'Salesforce — Corporate. 1 operations. Lead operation: Salesforce Corporate Member Enrollments. Self-contained
    Naftiko capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Corporate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-corporate
    baseUri: https://login.salesforce.com
    description: Salesforce — Corporate business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-loyalty-programs-loyaltyProgramName-corporate-member-enrollments
      path: /data/v64.0/loyalty-programs/{loyaltyProgramName}/corporate-member-enrollments
      operations:
      - name: corporatememberenrollments
        method: POST
        description: Salesforce Corporate Member Enrollments
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: loyaltyProgramName
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-corporate-rest
    port: 8080
    description: REST adapter for Salesforce — Corporate. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/data/v64-0/loyalty-programs/{loyaltyprogramname}/corporate-member-enrollments
      name: data-v64-0-loyalty-programs-loyaltyprogramname-corporate-member-enrollments
      description: REST surface for data-v64.0-loyalty-programs-loyaltyProgramName-corporate-member-enrollments.
      operations:
      - method: POST
        name: corporatememberenrollments
        description: Salesforce Corporate Member Enrollments
        call: salesforce-corporate.corporatememberenrollments
        with:
          loyaltyProgramName: rest.loyaltyProgramName
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-corporate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Corporate. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-corporate-member-enrollments
      description: Salesforce Corporate Member Enrollments
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-corporate.corporatememberenrollments
      with:
        loyaltyProgramName: tools.loyaltyProgramName
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
