naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Bundles
  description: 'Salesforce — Bundles. 1 operations. Lead operation: Salesforce Request Product Information (no Bundles). Self-contained
    Naftiko capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Bundles
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-bundles
    baseUri: https://login.salesforce.com
    description: Salesforce — Bundles business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-commerce-catalog-products-actions-get-products
      path: /data/v64.0/commerce/catalog-products/actions/get-products
      operations:
      - name: requestproductinformationnobundles
        method: POST
        description: Salesforce Request Product Information (no Bundles)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-bundles-rest
    port: 8080
    description: REST adapter for Salesforce — Bundles. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/data/v64-0/commerce/catalog-products/actions/get-products
      name: data-v64-0-commerce-catalog-products-actions-get-products
      description: REST surface for data-v64.0-commerce-catalog-products-actions-get-products.
      operations:
      - method: POST
        name: requestproductinformationnobundles
        description: Salesforce Request Product Information (no Bundles)
        call: salesforce-bundles.requestproductinformationnobundles
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-bundles-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Bundles. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-request-product-information-no
      description: Salesforce Request Product Information (no Bundles)
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-bundles.requestproductinformationnobundles
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
