naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Sync
  description: 'Salesforce — Sync. 1 operations. Lead operation: Salesforce Tooling Run Tests Sync. Self-contained Naftiko
    capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Sync
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-sync
    baseUri: https://login.salesforce.com
    description: Salesforce — Sync business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-tooling-runTestsSynchronous
      path: /data/v64.0/tooling/runTestsSynchronous
      operations:
      - name: toolingruntestssync
        method: POST
        description: Salesforce Tooling Run Tests Sync
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-sync-rest
    port: 8080
    description: REST adapter for Salesforce — Sync. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/data/v64-0/tooling/runtestssynchronous
      name: data-v64-0-tooling-runtestssynchronous
      description: REST surface for data-v64.0-tooling-runTestsSynchronous.
      operations:
      - method: POST
        name: toolingruntestssync
        description: Salesforce Tooling Run Tests Sync
        call: salesforce-sync.toolingruntestssync
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-sync-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Sync. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-tooling-run-tests-sync
      description: Salesforce Tooling Run Tests Sync
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-sync.toolingruntestssync
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
