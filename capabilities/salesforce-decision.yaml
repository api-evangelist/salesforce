naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Decision
  description: 'Salesforce — Decision. 1 operations. Lead operation: Salesforce Decision Model Notation Export. Self-contained
    Naftiko capability covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Decision
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-decision
    baseUri: https://login.salesforce.com
    description: Salesforce — Decision business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-connect-business-rules-decision-models-export
      path: /data/v64.0/connect/business-rules/decision-models/export
      operations:
      - name: decisionmodelnotationexport
        method: POST
        description: Salesforce Decision Model Notation Export
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-decision-rest
    port: 8080
    description: REST adapter for Salesforce — Decision. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/data/v64-0/connect/business-rules/decision-models/export
      name: data-v64-0-connect-business-rules-decision-models-export
      description: REST surface for data-v64.0-connect-business-rules-decision-models-export.
      operations:
      - method: POST
        name: decisionmodelnotationexport
        description: Salesforce Decision Model Notation Export
        call: salesforce-decision.decisionmodelnotationexport
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-decision-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Decision. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-decision-model-notation-export
      description: Salesforce Decision Model Notation Export
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-decision.decisionmodelnotationexport
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
