naftiko: 1.0.0-alpha2
info:
  label: Salesforce — Runs
  description: 'Salesforce — Runs. 3 operations. Lead operation: Salesforce Run Agent Test. Self-contained Naftiko capability
    covering one Salesforce business surface.'
  tags:
  - Salesforce
  - Runs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SALESFORCE_API_KEY: SALESFORCE_API_KEY
capability:
  consumes:
  - type: http
    namespace: salesforce-runs
    baseUri: https://login.salesforce.com
    description: Salesforce — Runs business capability. Self-contained, no shared references.
    resources:
    - name: data-v64.0-einstein-ai-evaluations-runs
      path: /data/v64.0/einstein/ai-evaluations/runs
      operations:
      - name: runagenttest
        method: POST
        description: Salesforce Run Agent Test
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: data-v64.0-tooling-runTestsAsynchronous
      path: /data/v64.0/tooling/runTestsAsynchronous
      operations:
      - name: toolingruntestsasync
        method: POST
        description: Salesforce Tooling Run Tests Async
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: data-v64.0-tooling-runTestsSynchronous
      path: /data/v64.0/tooling/runTestsSynchronous
      operations:
      - name: toolingruntestssync
        method: POST
        description: Salesforce Tooling Run Tests Sync
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SALESFORCE_API_KEY}}'
  exposes:
  - type: rest
    namespace: salesforce-runs-rest
    port: 8080
    description: REST adapter for Salesforce — Runs. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/data/v64-0/einstein/ai-evaluations/runs
      name: data-v64-0-einstein-ai-evaluations-runs
      description: REST surface for data-v64.0-einstein-ai-evaluations-runs.
      operations:
      - method: POST
        name: runagenttest
        description: Salesforce Run Agent Test
        call: salesforce-runs.runagenttest
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/tooling/runtestsasynchronous
      name: data-v64-0-tooling-runtestsasynchronous
      description: REST surface for data-v64.0-tooling-runTestsAsynchronous.
      operations:
      - method: POST
        name: toolingruntestsasync
        description: Salesforce Tooling Run Tests Async
        call: salesforce-runs.toolingruntestsasync
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/data/v64-0/tooling/runtestssynchronous
      name: data-v64-0-tooling-runtestssynchronous
      description: REST surface for data-v64.0-tooling-runTestsSynchronous.
      operations:
      - method: POST
        name: toolingruntestssync
        description: Salesforce Tooling Run Tests Sync
        call: salesforce-runs.toolingruntestssync
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: salesforce-runs-mcp
    port: 9090
    transport: http
    description: MCP adapter for Salesforce — Runs. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: salesforce-run-agent-test
      description: Salesforce Run Agent Test
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-runs.runagenttest
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-tooling-run-tests-async
      description: Salesforce Tooling Run Tests Async
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-runs.toolingruntestsasync
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: salesforce-tooling-run-tests-sync
      description: Salesforce Tooling Run Tests Sync
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: salesforce-runs.toolingruntestssync
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
